if (process.env.NODE_ENV !== 'production') {
    require('dotenv').load();
}

const NeoCities = require('neocities');
// const api = new NeoCities('learn2type', process.env.NEO_PW);

// api.upload([{ name: 'index.html', path: 'dist/index.html' }], function(resp) {
//     console.log(resp);
// });
